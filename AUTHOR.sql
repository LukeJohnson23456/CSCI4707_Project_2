CREATE TABLE AUTHOR (
    AuthorID        INT             NOT NULL,
    First_name      VARCHAR(15),
    Last_name       VARCHAR(15), 
    Bio             VARCHAR(1000),
    PRIMARY KEY(AuthorID)

);
